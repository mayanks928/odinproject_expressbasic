<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Home</title>
  </head>
  <body>
    <h1>Message Board</h1>
    <ul>
    <% for(let i=0;i<messages.length;i++){ %>
      <li>
        <p>User:<%= messages[i].username %>, Date:<%= messages[i].datetime %></p>
        <p>Message: <%= messages[i].message %></p>
        <a href=<%= `message/${messages[i].id}` %>><button>Open Message</button></a>
      </li>
      <% } %>
      </ul>
    <a href="/new">Add New Message</a>
  </body>
</html>
